/** Bump this on each Minorities release (GitHub Pages). */
window.MIN_APP_VERSION = "0.0.0.4";

window.MIN_APP_VERSION_LABEL = "v" + window.MIN_APP_VERSION;

console.log("[The Minorities] working version:", window.MIN_APP_VERSION_LABEL);
